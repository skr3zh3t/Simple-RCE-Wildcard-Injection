services:
  rce-lab:
    build: .
    container_name: jsp_polyglot_lab
    ports:
      - "8080:8080"
    environment:
      - USER_FLAG=practice{jsp_polyglot_upload_bypass}
      - ROOT_FLAG=practice{jrunscript_sudo_privesc_master}
    restart: unless-stopped